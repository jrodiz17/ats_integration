pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Liveramp repository
        maven {
            url "https://sdk-android-dev.console.fktr.io/"
        }
    }
}
rootProject.name = "ATS_Integration"
include ':app'
